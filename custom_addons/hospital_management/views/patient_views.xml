<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hospital_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="patient_id" />
                <field name="full_name" />
                <field name="contact_number" />
                <field name="last_visit_date" />
                <field name="next_appointment" />
                <field name="status" />
            </tree>
        </field>
    </record>

    <record id="view_hospital_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Patients">
                <sheet>
                    <group>
                        <group>
                            <field name="patient_id" />
                            <field name="full_name" />
                            <field name="contact_number" />
                        </group>
                        <group>
                            <field name="last_visit_date" />
                            <field name="next_appointment" />
                            <field name="status" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hospital_patient_kanban" model="ir.ui.view">
        <field name="name">hospital.patient.kanban</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <kanban default_group_by="status" sample="1">
                <field name="id"/>
                <field name="full_name"/>
                <field name="patient_id"/>
                <field name="contact_number"/>
                <field name="status"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_res_partner_kanban">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="full_name"/>
                                </strong>
                                <div class="mt-1">
                                    <span>ID: <field name="patient_id"/></span>
                                </div>
                                <div>
                                    <span>Contact: <field name="contact_number"/></span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_hospital_patient_graph" model="ir.ui.view">
        <field name="name">hospital.patient.graph</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <graph string="Patient Status" type="pie" sample="1">
                <field name="status" type="row"/>
            </graph>
        </field>
    </record>

    <record id="action_hospital_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,graph,form</field>
        <field name="type">ir.actions.act_window</field>
    </record>

</odoo>
